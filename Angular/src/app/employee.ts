import { SalesEmployee } from "./sales-employee";

export class Employee {
    id: number;
    employeeName: string;
    employeeaddress: string;
    employeenin: string;
    employeeBank: string;
    employeeSalary: number;

    
/*
    //sales emp -> emp
    constructor(sEmp ?: SalesEmployee)
        {
           
            if(sEmp.name != null){
            this.name = sEmp.name;
            }

            if(sEmp.id != null && sEmp.id != 0){
            this.id = sEmp.id;
            }

            if(sEmp.address != null){
            this.address = sEmp.address;
            }

            if(sEmp.nin != null){
            this.nin = sEmp.nin;
            }


            if(sEmp.bankNumber != null){
            this.bankNumber = sEmp.bankNumber;
            }


            if(sEmp.salary != null){
            this.salary = sEmp.salary;
            }
        } */

    }
